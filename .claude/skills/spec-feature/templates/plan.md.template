# Plan: {{feature_name}}

> Status: Draft — pending architect review.
> Depends on: this feature's `spec.md` (must be approved).

## Review summary

- **Decisions needing approval**: ...
- **Deviations from the baseline/original exercise**: ...
- **Key risks**: ...

## Technical approach

Description of how what's defined in `spec.md` gets implemented: AWS
resources involved, Terraform file structure (`envs/dev/...`,
`modules/...` if applicable), and organization of Python code in
`src/lambdas/` where relevant.

## Files and responsibilities

Short list of the files this feature adds/touches and what each is
responsible for — not an exhaustive resource-attribute table. The code is
the source of truth for resource attributes; call out only non-obvious
resources, naming decisions, or cross-file dependencies here.

- `envs/dev/{{file}}.tf` — {{responsibility}}

## Architecture decisions (ADRs)

### ADR-1: {{title}}

- **Context**: ...
- **Decision**: ...
- **Alternatives considered**: {{one line}}
- **Consequences**: ...

> Note: any IAM least-privilege decision (not using `Resource: "*"`) must
> be documented here as an ADR, per the project's non-negotiable rule #4.

## Variables and configuration

New environment variables / Terraform variables introduced by this
feature (names, types, defaults, sensitivity).

## Validation

How this feature is validated before requesting approval (always:
`terraform fmt`, `terraform validate`, `terraform plan` — never `apply`,
which is deferred to the architect). Note any manual-apply caveats here
(e.g. a resource that stays pending until a human completes a step
Terraform can't, like an SNS email confirmation).

## Risks / trade-offs

- ...
